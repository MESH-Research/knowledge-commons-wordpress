server {
    listen 80 default_server;
    server_name _;

    root /app/site/web;
    index index.php index.htm index.html;

    add_header X-Content-Type-Options nosniff;

    charset utf-8;
    client_max_body_size 25m;

    # Allow for URLs without /wp/ prefix
    rewrite ^/(wp-.*.php)$ /wp/$1 last;
    rewrite ^/(wp-(content|admin|includes).*) /wp/$1 last;

    # Prevent PHP scripts from being executed inside the uploads folder.
    location ~* /app/uploads/.*\.php$ {
        deny all;
    }

    # Mirror the production rule in
    # config/all/nginx/templates/00-hcommons.conf.template: route Doc file
    # requests through WordPress so each Doc's "read" permission is enforced.
    location ~ ^/app/uploads/(?:sites/[0-9]+/)?bp-attachments/([0-9]+)/(.+)$ {
        return 302 $scheme://$host/?p=$1&bp-attachment=$2;
    }

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        fastcgi_split_path_info ^(.+?\.php)(/.*)$;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_buffers 256 128k;
        fastcgi_connect_timeout 300s;
        fastcgi_send_timeout 300s;
        fastcgi_read_timeout 300s;
        fastcgi_pass app:9000;
    }
}
